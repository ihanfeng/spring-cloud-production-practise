spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/passport?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: iamchenjie
  redis:
    host: localhost
    port: 6379
  cloud:
    consul:
      enabled: false

common-service:
  ribbon:
    listOfServers: localhost:7000